
<table>
  <h3><%= @estimate.customer_name %> 御中</h3>
</table>

<table>
  <tr>
    <th>製品名</th>
    <th>数量</th>
    <th>単価</th>
    <th>合計</th>
  </tr>
  </table>
<table>
<% @estimate.products.each do | pro | %>
<tr>
  <td><%= pro.name %></td>
  <td>2</td>
  <td><%= pro.unit_price %></td>
  <td><%= pro.unit_price * 2 %></td>
</tr>
<% end %>
</table>

<table>
<p>納期: <%= @estimate.deadline %></p>
<p class="text-danger">値引: <%= @estimate.discount %></p>
<% if @estimate.discount == nil %>
  <p>金額: <%= @estimate.products.sum(:unit_price) %></p>
  <p>税込: <%= @estimate.products.sum(:unit_price)*1.1 %></p>
<% else %>
  <p>金額: <%= @estimate.products.sum(:unit_price)-@estimate.discount %></p>
  <p>税込: <%= (@estimate.products.sum(:unit_price)-@estimate.discount)*1.1 %></p>
<% end %>
</table>


  <p>値引き理由</p>
  <!-- このreasons_areaというHTMLのidを取得して、そのdivの部分だけを更新する -->
  <div id="reasons_area">
    <!-- 投稿されたコメント一覧をブログの詳細ページに表示するためのrender -->
    <%= render partial: 'reasons/index', locals: { reasons: @reasons, estimate: @estimate } %>
  </div>
  <!-- コメント入力欄をブログの詳細ページに表示するためのrender -->
  <%= render partial: 'reasons/form', locals: { reason: @reason, estimate: @estimate } %>
  <%= link_to '見積書編集', edit_estimate_path(@estimate), class: "btn btn-outline-warning" %>


<%= link_to '見積書一覧画面に戻る', estimates_path %>
